package com.newiron.ideaflow.data

//should be configurable, but come with defaults if not configured...
enum MistakeType {
    missingAChange,
    unexpectedDependency,
    complexity,
    controlMistake,
    familiarity,
}
