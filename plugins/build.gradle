apply plugin: 'java'

repositories {
	mavenCentral()
}

dependencies {
	compile 'org.codehaus.groovy:groovy-all:2.1.6'
}

task prepareLibs(type: Copy) {
	into "common/libs"
	from configurations.runtime
}